; variable test for Ltran

proc print (setarg "str" string) (setarg "len" i64) do
    syscall i64.1 i64.1 (strptr (fetchls "str")) (fetchli "len")
    ret i64.1
corp

proc main do
    set "test" "hello world" string
    print (fetch "test" string)
    ret i64.1
corp
